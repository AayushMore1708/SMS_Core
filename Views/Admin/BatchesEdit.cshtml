@model SMS_Core.Models.tblCourseBatch
@{
    // Layout = null;

}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="keywords" content="jQuery Splitter, Splitter Widget, Splitter, jqxSplitter" />
    <meta name="description" content="This page demonstrates splitter's events" />
    <title id='Description'>Edit Batches</title>
    <link rel="stylesheet" href="~/Content/css/chosen.css">

    <script type="text/javascript">
        $(document).ready(function () {

            $("#BatchStartDate").datepicker({
                changeMonth: true,
                changeYear: true,
                dateFormat: 'dd-M-yy'
            });

            $("#BatchEndDate").datepicker({
                changeMonth: true,
                changeYear: true,
                dateFormat: 'dd-M-yy'
            });

            // $('#footer').hide();
            $('#sav').click(function (event) {
                if ($('#BatchName').val() != "") {
                    if (!confirm('Are you sure that you want to Save')) {
                        event.preventDefault();
                    }
                    else {
                        $('#frm').submit();
                        return false;

                    }
                }
                else {
                    alert("Cource Name should not be blank")
                }
            });

            $('#frmsave').click(function (event) {
                if ($('#BatchName').val() != "") {
                    if (!confirm('Are you sure that you want to Save'))
                        event.preventDefault();
                }
                else {
                    alert("Cource Name should not be blank")
                }
            });


        });

    </script>
</head>
<body>
    <div style="font-size:12px;height:40px;vertical-align:top;margin-left:10px">
        <img src="~/_imgs/grid/SaveAndClose_16.png" />&nbsp;&nbsp;<a href="#" id="sav" class="btn btn-primary">SAVE & CLOSE</a>
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;

    </div>


    <h3>&nbsp;&nbsp;Batches Edit</h3>

    @using (Html.BeginForm("BatchesEdit", "Admin", FormMethod.Post, new { enctype = "multipart/form-data", id = "frm", @class = "form-horizontal" }))
    {
        <div class="panel">
            <div class="panel-body">

                <div class="form-group">
                    <label class="control-label col-lg-2">Cource Name</label>
                    <div class="col-lg-4">
                        @Html.DropDownListFor(model => model.CourseName, new SelectList(ViewBag.crc, "AcadamicCourceName", "AcadamicCourceName"), new { @class = "form-control input-sm" })
                    </div>
                </div>

                <div class="form-group">
                    <label class="control-label col-lg-2">Batch Name</label>
                    <div class="col-lg-4">
                        @Html.TextBoxFor(model => model.BatchName, new { @class = "form-control input-sm" })
                        @Html.HiddenFor(model => model.BatchId)
                    </div>
                </div>

                <div class="form-group">
                    <label class="control-label col-lg-2">Batch Start Date</label>
                    <div class="col-lg-4">
                        @Html.TextBoxFor(model => model.BatchStartDate, new { @class = "form-control input-sm" })
                    </div>
                </div>

                <div class="form-group">
                    <label class="control-label col-lg-2">Batch End Date</label>
                    <div class="col-lg-4">
                        @Html.TextBoxFor(model => model.BatchEndDate, new { @class = "form-control input-sm" })
                    </div>
                </div>

                <div class="form-group">
                    <label class="control-label col-lg-2">Maximum Number of Students</label>
                    <div class="col-lg-4">
                        @Html.TextBoxFor(model => model.MaximumStudent, new { @class = "form-control input-sm" })
                    </div>
                </div>

                <div class="form-group">
                    <label class="control-label col-lg-2">Active</label>
                    <div class="col-lg-2">
                        @Html.DropDownListFor(model => model.Active, new SelectList(new[] { "Yes", "No" }), new { @class = "form-control input-sm chosen-select" })

                    </div>
                </div>

                <div class="form-group">
                    <label for="tags" class="control-label col-lg-4">&nbsp;</label>
                    <div class="col-lg-8">
                        <input type="submit" id="frmsave" value="Update & Close" class="btn btn-primary" />
                        <button class="btn btn-primary"><i class="icon-refresh icon-white"></i> Refresh</button>
                    </div>
                </div>
            </div>
        </div>
    }

    <script src="~/Content/js/chosen.jquery.js" type="text/javascript"></script>

    <script type="text/javascript">
        var config = {
            '.chosen-select': {},
            '.chosen-select-deselect': { allow_single_deselect: true },
            '.chosen-select-no-single': { disable_search_threshold: 10 },
            '.chosen-select-no-results': { no_results_text: 'Oops, nothing found!' },
            '.chosen-select-width': { width: "100%" }
        }
          //for (var selector in config) {
          //    $(selector).chosen(config[selector]);
         // }
    </script>
</body>
</html>