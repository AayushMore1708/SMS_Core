 @model IEnumerable<SMS_Core.Models.tblLessonPlanningDT>
<script type="text/javascript">

     var onSuccessd = function (result) {
         alert("Record Has Been Deleted Successfully !!");
         var LessonPlanningId1 = $('#LessonPlanningId').val();


         $.ajax({
             url: '@Url.Action("LessonPlanningNewDTView")',
             type: 'GET',
             cache: false,
             data: { LessonPlanningId1: LessonPlanningId1 },
             success: function (result) {
                 $('#partialPlaceHolder1').html(result);
             }
         });
      }

     var onFailure = function (result) {
         alert("fail");
         $("#partialPlaceHolder1").html("Oops There is error, please close and restart application");
     }



</script>
<style>

    .MyCssClass
{
  background:url(@Url.Content("~/Content/ico/delete.png")) no-repeat;display:block; height:24px; width:24px;
}
</style>

<table class="table   table-striped">
    <thead class="text-info">
        <tr class="text-info">
            
            <th class="text-left">Lesson Code</th>
            <th class="text-left">Lesson Topic</th> 
            <th class="text-left">Action</th>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model)
        {
            <tr>

                <td>@Html.DisplayFor(modelItem => item.LessonPlanningLectureCode)</td>
                <td>@Html.DisplayFor(modelItem => item.LessonPlanningLectureTopic)</td> 
                <td>
                    @* @Ajax.ActionLink(" ", "DeleteLessonPlanningDT", new { id = item.LessonPlanningDTId, LessonPlanningId1 = item.LessonPlanningId }, new AjaxOptions { HttpMethod = "POST", OnSuccess = "onSuccessd", Confirm = "Do you want to delete this record?" }, new { @class = "MyCssClass" }) *@

                </td>

            </tr>
        }

    </tbody>
</table>